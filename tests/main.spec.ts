import { describe, expect, it } from "@jest/globals";

describe("main", () => {
  it("should math result be the sum between two values", () => {
    const sum = 1 + 1;
    expect(sum).toBe(2);
  });
});
